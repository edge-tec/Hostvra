version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: hostvra-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: hostvra
      POSTGRES_PASSWORD: hostvra_dev_password
      POSTGRES_DB: hostvra
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hostvra -d hostvra"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: hostvra-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
